package com.example.sns.repository.ajout.activities;

import com.example.sns.model.ajout.activities.ADDCommentsModel;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ADDCommentsRepository extends JpaRepository<ADDCommentsModel, Long> {
}
