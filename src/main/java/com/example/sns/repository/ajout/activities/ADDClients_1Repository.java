package com.example.sns.repository.ajout.activities;

import com.example.sns.model.ajout.activities.ADDClients_1Model;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ADDClients_1Repository extends JpaRepository<ADDClients_1Model, Long> {
}
